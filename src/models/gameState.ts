import { GamePhase } from '../constants/game'; import type { Cell } from './cell'; import type { Player } from './player'; export interface GameState { id:string; room_id:string; tick:number; phase:GamePhase; countdown_seconds:number; map:Cell[][]; players:Player[]; leaderboards:{player_id:string;score:number}[]; start_time:string; end_time?:string }
