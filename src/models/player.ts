import { PlayerStatus } from '../constants/player'; import { PlayerDirection } from '../constants/game'; import type { Cell } from './cell'; export interface Player { id: string; nickname: string; color: string; score: number; status: PlayerStatus; position: { x: number; y: number }; direction: PlayerDirection; trail: Cell[]; territory: Cell[] }
