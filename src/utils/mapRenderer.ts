import type { GameState } from '../models/gameState'; import { CellType } from '../constants/cell'; import { MAP_CONFIG } from '../constants/map'; import { PlayerDirection, GamePhase } from '../constants/game'; import { logGame } from './gameLogger'; export function renderMap(canvas:HTMLCanvasElement,state:GameState){ const ctx=canvas.getContext('2d'); if(!ctx) return; canvas.width=MAP_CONFIG.width*MAP_CONFIG.cellSize; canvas.height=MAP_CONFIG.height*MAP_CONFIG.cellSize; ctx.fillStyle='#edf7ff'; ctx.fillRect(0,0,canvas.width,canvas.height); for(const row of state.map){ for(const cell of row){ ctx.strokeStyle='#d5e8f5'; ctx.strokeRect(cell.x*MAP_CONFIG.cellSize,cell.y*MAP_CONFIG.cellSize,MAP_CONFIG.cellSize,MAP_CONFIG.cellSize); if(cell.type!==CellType.EMPTY){ ctx.fillStyle=cell.type===CellType.TRAIL?'#f2c94c':'#9fd3ff'; ctx.fillRect(cell.x*MAP_CONFIG.cellSize+2,cell.y*MAP_CONFIG.cellSize+2,MAP_CONFIG.cellSize-4,MAP_CONFIG.cellSize-4); } } } for(const p of state.players){ const cx=(p.position.x+.5)*MAP_CONFIG.cellSize; const cy=(p.position.y+.5)*MAP_CONFIG.cellSize; ctx.fillStyle=p.color; ctx.beginPath(); ctx.arc(cx,cy,7,0,Math.PI*2); ctx.fill(); const showDir=state.phase===GamePhase.WAITING||state.phase===GamePhase.COUNTDOWN; if(showDir){ ctx.save(); ctx.translate(cx,cy); ctx.fillStyle=p.color; ctx.beginPath(); const s=MAP_CONFIG.cellSize; const headLen=s*0.35; const headW=s*0.18; let angle=0; switch(p.direction){ case PlayerDirection.UP:angle=-Math.PI/2;break; case PlayerDirection.DOWN:angle=Math.PI/2;break; case PlayerDirection.LEFT:angle=Math.PI;break; case PlayerDirection.RIGHT:angle=0;break; } ctx.rotate(angle); ctx.moveTo(8,0); ctx.lineTo(8+headLen-headW,-headW); ctx.lineTo(8+headLen,0); ctx.lineTo(8+headLen-headW,headW); ctx.closePath(); ctx.fill(); ctx.strokeStyle='#fff'; ctx.lineWidth=1; ctx.stroke(); ctx.restore(); } } if(state.phase===GamePhase.COUNTDOWN){ ctx.fillStyle='rgba(242,201,76,0.08)'; ctx.fillRect(0,0,canvas.width,canvas.height); } logGame('MAP_RENDER',{tick:state.tick}); }
